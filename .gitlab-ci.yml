variables:
  SIMPLEIPTV_VERSION: "1.0.1"
  GIT_SUBMODULE_STRATEGY: recursive

workflow:
  rules:
    - if: $CI_COMMIT_TAG
      when: never
    - when: always

stages:
  - version
  - build
  - publish

bump-version:
  stage: version
  tags:
    - linux
  only:
    - master
  script: |
    #!/bin/bash
    set -euo pipefail
    scripts/bump_version.sh
    echo "Version bumped to: $(cat version.txt)"
  artifacts:
    expire_in: 2 hours
    paths:
      - version.txt

build-linux:
  stage: build
  tags:
    - linux
  only:
    - master
  needs:
    - job: bump-version
      artifacts: true
  script:
    - echo "Building Linux artifacts..."
    - scripts/build_linux.sh
    - echo "Build complete."
  artifacts:
    expire_in: 6 hours
    paths:
      - packages/
    reports:
      dotenv: build.env

build-windows:
  stage: build
  tags:
    - windows
  only:
    - master
  needs:
    - job: bump-version
      artifacts: true
  script:
    - powershell -File scripts/build_windows.ps1
  artifacts:
    expire_in: 6 hours
    paths:
      - packages/

publish:
  stage: publish
  tags:
    - linux
  only:
    - master
  needs:
    - job: bump-version
      artifacts: true
    - job: build-linux
      artifacts: true
    - job: build-windows
      artifacts: true
  script: |
    set -x
    VERSION=$(cat version.txt)
    echo "Publishing SimpleIPTV version $VERSION..."

    for f in packages/*; do
      if [ -f "$f" ]; then
        filename=$(basename "$f")
        full_path=$(realpath "$f")

        echo "Uploading: $filename"

        curl --location --header "JOB-TOKEN: ${CI_JOB_TOKEN}" \
          --upload-file "$full_path" \
          "${CI_API_V4_URL}/projects/${CI_PROJECT_ID}/packages/generic/simpleiptv/${VERSION}/${filename}"
      fi
    done

    echo "Publish complete!"

publish-fedora-repo:
  stage: publish
  tags:
    - linux
  needs:
    - job: build-linux
      artifacts: true
  before_script:
    - mkdir -p ~/.ssh
    - chmod 700 ~/.ssh
    - ssh-keyscan -H fedora-repo > ~/.ssh/known_hosts
    - cp "$REPO_SSH_KEY" ~/.ssh/id_ed25519
    - chmod 600 ~/.ssh/id_ed25519

  script:
    - |
      shopt -s nullglob
      rpms=(packages/*.rpm)

      if [ ${#rpms[@]} -eq 0 ]; then
        echo "No Fedora RPMs produced; skipping repo publish."
        exit 0
      fi
      echo "Publishing Fedora RPMs for Fedora ${OS_VERSION}"
      RPM_DIR="packages"
      REPO_PATH="/srv/rpmrepo/fedora/${OS_VERSION}/x86_64"

      ssh repo@fedora-repo "mkdir -p ${REPO_PATH}"

      # Copy only RPMs
      rsync -av "${rpms[@]}" repo@fedora-repo:${REPO_PATH}/

      # Update repository metadata
      ssh repo@fedora-repo "
          createrepo_c --update ${REPO_PATH} ||
          createrepo_c ${REPO_PATH}
        "
